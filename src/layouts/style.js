import styled from 'styled-components';

export const LayoutWraper = styled.div`
  /* position: relative; */
  display: flex;
  .layout-right {
    padding: 0 20px;
    position: relative;
    flex: 1;
  }
`;
